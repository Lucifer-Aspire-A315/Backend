# Example environment variables for RN FinTech Backend

# Database
DATABASE_URL="postgresql://postgres:password@localhost:5432/rn_fintech?schema=public"

# JWT
JWT_SECRET=your_jwt_secret_here
JWT_EXPIRES_IN=1h

# Cloudinary (for KYC uploads)
CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret
CLOUDINARY_KYC_FOLDER=rn_fintech_kyc

# KYC Settings
KYC_MAX_FILE_SIZE=5242880  # 5MB
KYC_ALLOWED_TYPES=image/jpeg,image/png,application/pdf

# Redis (optional, for OTP/sessions)
REDIS_URL=redis://localhost:6379

# App
NODE_ENV=development
```bash
# Example environment variables for RN FinTech Backend

# Database
DATABASE_URL="postgresql://postgres:password@localhost:5432/rn_fintech?schema=public"

# JWT
JWT_SECRET=your_jwt_secret_here
JWT_EXPIRES_IN=1h

# Cloudinary (for KYC uploads)
CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret
CLOUDINARY_KYC_FOLDER=rn_fintech_kyc

# KYC Settings
KYC_MAX_FILE_SIZE=5242880  # 5MB
KYC_ALLOWED_TYPES=image/jpeg,image/png,application/pdf

# Redis (optional, for OTP/sessions)
REDIS_URL=redis://localhost:6379

# App
NODE_ENV=development
PORT=3000

# Frontend
FRONTEND_URL=http://localhost:3000

# Email provider (Resend preferred)
# If using Resend, set RESEND_API_KEY and EMAIL_FROM
RESEND_API_KEY=your_resend_api_key_here
EMAIL_FROM=no-reply@yourdomain.com

# SMTP fallback (optional)
SMTP_HOST=smtp.yourprovider.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=smtp-user
SMTP_PASS=smtp-pass
SMTP_FROM=no-reply@yourdomain.com

# Development helpers
# Set DEV_SEND_EMAIL=true to allow real email sending in development (use with caution)
DEV_SEND_EMAIL=false

```
